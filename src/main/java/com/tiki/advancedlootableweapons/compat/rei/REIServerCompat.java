package com.tiki.advancedlootableweapons.compat.rei;

import me.shedaniel.rei.api.common.plugins.REIServerPlugin;
import me.shedaniel.rei.forge.REIPluginCommon;

@REIPluginCommon
public class REIServerCompat implements REIServerPlugin {

   // @Override
  //  public void registerDisplaySerializer(DisplaySerializerRegistry registry) {
   //     registry.register(AlloyFurnaceCategory.CATEGORY_IDENTIFIER,AlloyFurnaceDisplay.serializer(recipe -> new AlloyFurnaceDisplay(recipe)));

    //}
}
